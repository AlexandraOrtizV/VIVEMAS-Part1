@model VIVEMAS.Models.Rutina

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Rutina</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Platillo_id" class="control-label"></label>
                <select asp-for="Platillo_id" class ="form-control" asp-items="ViewBag.Platillo_id"></select>
            </div>
            <div class="form-group">
                <label asp-for="Ejercicio_id" class="control-label"></label>
                <select asp-for="Ejercicio_id" class ="form-control" asp-items="ViewBag.Ejercicio_id"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
