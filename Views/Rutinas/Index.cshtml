@model IEnumerable<VIVEMAS.Models.Rutina>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Platillo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ejercicio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Platillo.Platillo_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ejercicio.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Rutina_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Rutina_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Rutina_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
